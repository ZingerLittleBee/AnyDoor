import SwiftUI

/// Placeholder translation UI. The full implementation (input field, language
/// bar, stacked service cards, pin button) lands in Task 21; this stub exists so
/// `TranslationWindowController` (Task 17) compiles and can be exercised
/// standalone. Task 21 replaces this view entirely.
struct TranslationView: View {
    let controller: TranslationWindowController

    var body: some View {
        Color.clear
    }
}
