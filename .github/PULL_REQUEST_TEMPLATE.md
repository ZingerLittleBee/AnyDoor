<!-- Please write the PR title and description in English, and keep the title
     in Conventional Commits style, e.g. `fix(clipboard): handle empty pasteboard`. -->

## Summary

<!-- What does this PR change? -->

## Motivation

<!-- Why is this change needed? Link the related issue if there is one, e.g. "Fixes #123". -->

## How was this tested?

<!-- Check all that apply and describe manual verification steps —
     hotkeys, capture, and menu-bar UI can only be exercised by running the app. -->

- [ ] `swift build` passes
- [ ] `swift test` passes
- [ ] Manually verified:

## Checklist

- [ ] Commit messages are in English and follow [Conventional Commits](https://www.conventionalcommits.org/)
- [ ] Code comments are in English; UI-facing strings are in Chinese via `L10n` / the `.xcstrings` catalog
- [ ] User-visible changes have a `CHANGELOG.md` entry under `## [Unreleased]` (no hand-authored version headings)
- [ ] No new Swift 6 strict-concurrency warnings
